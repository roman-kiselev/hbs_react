import {
    check,
    getUser,
    loginIn,
    setIsAuth,
    setUser,
    userReducer,
    userSlice,
} from "./userSlice";

export { check, getUser, loginIn, setIsAuth, setUser, userReducer, userSlice };
