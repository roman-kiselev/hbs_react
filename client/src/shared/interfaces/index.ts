import {
    CreateHandler,
    IDataError,
    IMessage,
    IObject,
    IObjectSlice,
    IRole,
    IToken,
    IUser,
    IUserLoginAttr,
    IUserSlice,
} from "./models";

export type {
    CreateHandler,
    IDataError,
    IMessage,
    IObject,
    IObjectSlice,
    IRole,
    IToken,
    IUser,
    IUserLoginAttr,
    IUserSlice,
};
